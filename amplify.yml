version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use $VERSION_NODE_14
        - npm install
        - npm rebuild node-sass
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
